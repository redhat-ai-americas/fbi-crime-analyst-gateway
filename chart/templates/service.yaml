apiVersion: v1
kind: Service
metadata:
  name: {{ include "fbi-crime-analyst-gateway.fullname" . }}
  labels:
    {{- include "fbi-crime-analyst-gateway.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fbi-crime-analyst-gateway.selectorLabels" . | nindent 4 }}
